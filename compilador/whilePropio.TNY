{ Sample program
  in TINY language -
  computes factorial
}
read x; { input an integer }
if 0 < x then { don't compute if x <= 0 }
  prueba := 0;
  while prueba < x then
	prueba := prueba + prueba
  endwhile;
  write prueba  { output  }
end
